{
  "name": "graph",
  "version": "0.1.0",
  "scripts": {
    "docker-up": "docker-compose up -d --force-recreate --renew-anon-volumes",
    "docker-clean": "docker-compose kill && docker-compose -y rm",
    "pretest": "npm run docker-up",
    "prepare-rskTestnet": "mustache config/rskTestnet.json subgraph.template.yaml > subgraph.yaml",
    "test": "jest --runInBand",
    "logs": "docker-compose logs -f graph-node",
    "create": "graph create babelfish/graph --node http://127.0.0.1:8020",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy babelfish/graph --ipfs http://127.0.0.1:5001 --node http://127.0.0.1:8020"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.20.1",
    "@graphprotocol/graph-ts": "^0.20.0",
    "@typechain/ethers-v5": "^7.0.1",
    "@types/jest": "^26.0.20",
    "@types/mustache": "^4.1.2",
    "@types/node": "^16.4.0",
    "hardhat": "^2.0.10",
    "jest": "^27.5.1",
    "mustache": "^4.2.0",
    "ts-jest": "^27.0.4",
    "typechain": "^5.1.2"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0",
    "ethers": "^5.4.1"
  }
}
